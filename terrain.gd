extends Node3D
#
#var fauna =preload("res://Fauna.tscn")
#@onready var terrain: Node3D = $".."
#
#func _process(delta: float) -> void:
	  ## Wait for one frame so physics world is valid
	#var f = fauna.instantiate()
	#terrain.add_child(f) 
	#queue_free()
